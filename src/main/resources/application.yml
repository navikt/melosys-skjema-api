spring:
  application:
    name: melosys-skjema-api

server:
  port: 8080
  servlet:
    context-path: /api

logging:
  level:
    no.nav.melosys: INFO
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n"